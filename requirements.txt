# Schema.org File Organization System
# Requirements file - Generated 2025-12-10
# Python 3.8+ required (3.14 recommended)

# =============================================================================
# CORE DEPENDENCIES (Required for basic functionality)
# =============================================================================

# Image processing
Pillow>=12.0.0                    # Image loading, EXIF extraction, thumbnails
pillow_heif>=1.1.1                # HEIC/HEIF image support (iPhone photos)
piexif>=1.1.3                     # EXIF metadata read/write

# OCR text extraction
pytesseract>=0.3.13               # Python wrapper for Tesseract OCR
# Note: Also requires system install: brew install tesseract

# Geolocation
geopy>=2.4.1                      # Geocoding (GPS coords to location names)

# Database storage
SQLAlchemy>=2.0.0                 # ORM for graph-based file storage

# =============================================================================
# AI/ML DEPENDENCIES (Required for AI vision classification)
# =============================================================================

# Deep learning framework
torch>=2.9.0                      # PyTorch ML framework
torchvision>=0.24.0               # Computer vision utilities

# Transformer models
transformers>=4.57.0              # Hugging Face transformers (CLIP model)
huggingface-hub>=0.36.0           # Model downloading and caching

# Computer vision
opencv-python>=4.12.0             # OpenCV for image processing

# =============================================================================
# DOCUMENT PROCESSING (Optional - for document metadata)
# =============================================================================

python-docx>=1.2.0                # Microsoft Word document parsing
pypdf>=6.3.0                      # PDF text extraction
PyPDF2>=3.0.1                     # PDF utilities (legacy support)
pdf2image>=1.17.0                 # PDF to image conversion
openpyxl>=3.1.5                   # Excel spreadsheet parsing
lxml>=6.0.0                       # XML/HTML parsing

# =============================================================================
# ERROR TRACKING & MONITORING
# =============================================================================

sentry-sdk>=2.0.0                 # Sentry error tracking and performance monitoring

# =============================================================================
# UTILITIES
# =============================================================================

requests>=2.32.0                  # HTTP requests
tqdm>=4.67.0                      # Progress bars
PyYAML>=6.0.0                     # YAML parsing
numpy>=2.2.0                      # Numerical operations
scikit-learn>=1.5.0               # ML utilities (train/test split, vectorizers, encoders)
joblib>=1.3.0                     # Disk caching for CLIP embeddings (clip_cache.py)

# =============================================================================
# DEVELOPMENT DEPENDENCIES (Optional)
# =============================================================================

# Testing
# pytest>=7.4.0
# pytest-cov>=4.1.0

# Code quality
# black>=23.7.0
# flake8>=6.1.0
# mypy>=1.5.0

# =============================================================================
# SYSTEM DEPENDENCIES (Install separately)
# =============================================================================
#
# macOS:
#   brew install tesseract         # OCR engine
#   brew install poppler           # PDF rendering (for pdf2image)
#
# Linux:
#   sudo apt-get install tesseract-ocr
#   sudo apt-get install poppler-utils
#
# =============================================================================
