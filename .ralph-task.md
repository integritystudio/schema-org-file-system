# Ralph Task: Clear BACKLOG.md Items R3–R8

Work through all Open Items in `/Users/alyshialedlie/schema-org-file-system/docs/BACKLOG.md`.
For each item: implement it, then apply /simplify on each changed file.
One commit per item. After all items are done, mark them Done in BACKLOG.md.

## Items to implement

### R3 — Fix Image.open() file handle leak in image_content_renamer.py
File: `scripts/image_content_renamer.py` line 149
Fix: wrap `Image.open(image_path)` in a context manager so file handles are released.

### R4 — Document _ABBREV_TO_CONTENT first-match priority in organize_to_existing.py
File: `scripts/organize_to_existing.py` lines 50–53
Fix: add a comment above or within the for loop explaining first-match priority is intentional
and depends on `CONTENT_ABBREVIATIONS` insertion order (Python 3.7+ dict ordering guarantee).

### R5 — Update typing imports to modern syntax
Files:
- `scripts/analyze_renamed_files.py` line 14
- `scripts/image_content_renamer.py` line 12
Fix: replace `from typing import Dict, List, Optional, Tuple` with built-in generic types
(use `dict`, `list`, `tuple`, `str | None` etc.). Match the style already in `scripts/shared/ocr_utils.py`.

### R6 — Fix Pillow context manager semantics in ocr_utils.py
File: `scripts/shared/ocr_utils.py` lines 39–42 (inside `extract_ocr_text`)
Fix: rename the raw image variable to `raw_img` and assign the converted copy to `img`,
making it clear which object is context-managed vs which is the live copy being used.

### R7 — Add db_connection() auto-commit documentation
File: `scripts/shared/db_utils.py`
Fix: update the `db_connection()` docstring to explicitly state it does NOT auto-commit;
callers must call `conn.commit()` explicitly or use `with conn:` for transaction semantics.

### R8 — Add unit tests for scripts/shared/ module
File to create: `tests/unit/test_shared.py`
Cover: `resolve_collision` (from `shared.file_ops`), `get_db_connection`, `db_connection`
(from `shared.db_utils`), and `extract_ocr_text` (from `shared.ocr_utils`).
Use existing fixtures from `tests/conftest.py`: `temp_dir`, `temp_db_path`, `sample_image_file`.

## Procedure

For EACH item:
1. Read the relevant file(s)
2. Make the fix/addition
3. Run `/simplify` on changed files (review for quality, unnecessary complexity, style)
4. Run `cd /Users/alyshialedlie/schema-org-file-system && source venv/bin/activate && python -m pytest tests/unit/ -x -q 2>&1 | tail -20` to verify tests pass
5. Commit with message format: `fix(backlog): <item-id> — <short description>`

After ALL items are done and committed:
- Update `docs/BACKLOG.md`: change each item's **Status:** from `Pending` to `Done`
- Output the exact string: <promise>BACKLOG_CLEARED</promise>

## Completion promise
When all items are done and BACKLOG.md is updated: output `<promise>BACKLOG_CLEARED</promise>`
